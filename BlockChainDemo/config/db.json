{
    "DBCONFIG": [
        {
            "REGISTER": "INSERT INTO user (name,passwd,phone) VALUES (?,?,?)",
            "QueryRep": "SELECT FROM user WHERE name = ?",
            "LOGIN": "SELECT phone,passwd FROM user WHERE phone = ?"
        }
    ]
}